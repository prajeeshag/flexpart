stages:
  - build
  - test

default:
  tags:
    - podman

ubuntu-build:
  image: almalinux:8-minimal
  stage: build
  when: manual

  before_script:
    - microdnf install -y epel-release
    - microdnf install -y --enablerepo=powertools make netcdf-fortran-devel.x86_64 eccodes-devel.x86_64 netcdf.x86_64

  script:
    - export FC=gfortran
    - export LIBRARY_PATH=/usr/lib64
    - export CPATH=/usr/include:/usr/lib64/gfortran/modules
    - make -C src -f makefile_gfortran
    - ulimit -s unlimited
    - ./src/FLEXPART

  artifacts:
    paths:
      - ./src/FLEXPART
